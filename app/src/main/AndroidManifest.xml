<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.caloriecounter">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        tools:targetApi="31">

        <!-- Главный экран приложения с подневным учетом и календарем -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Экран добавления и редактирования приема пищи (выбор продукта и граммов) -->
        <activity
            android:name=".ui.AddEditActivity"
            android:exported="false"
            android:label="Запись приема пищи"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Экран детального просмотра информации о выбранном приеме пищи -->
        <activity
            android:name=".ui.DetailActivity"
            android:exported="false"
            android:label="Детали приема пищи"
            android:parentActivityName=".ui.MainActivity" />

        <!-- НОВЫЙ ЭКРАН: Управление локальной базой продуктов (БЖУК на 100г) -->
        <activity
            android:name=".ui.ManageProductsActivity"
            android:exported="false"
            android:label="База продуктов"
            android:parentActivityName=".ui.MainActivity" />

    </application>

</manifest>